# 软件详细设计文档
## 涡轮增压器测试台进气调节空调 (AHU) 设计计算系统

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-06-27 |
| 产品名称 | AHU 设计计算系统 |
| 产品类型 | Electron 桌面应用 |
| 编程语言 | 原生 JavaScript (Vanilla JS) |

---

## 1. 系统架构

### 1.1 整体架构

```
┌──────────────────────────────────────────────────┐
│                  Electron Main                    │
│                 main.js (117行)                    │
│   ┌──────────────┐    ┌──────────────────────┐   │
│   │ BrowserWindow │    │ IPC Handlers         │   │
│   │ 1200×800      │    │ save-excel-file      │   │
│   │ nodeInt=true  │    │ save-svg-file        │   │
│   │ ctxIso=false  │    │ save-pdf-file        │   │
│   └──────┬───────┘    └──────────────────────┘   │
└──────────┼───────────────────────────────────────┘
           │ load index.html
           ▼
┌──────────────────────────────────────────────────┐
│                Renderer Process                   │
│                                                    │
│  index.html (390行)                                │
│   ├── header (标题栏)                              │
│   ├── div.tab-bar (标签导航)                       │
│   └── 6× div.tab-content (各标签页)               │
│                                                    │
│  ├── calculations.js (432行) — 核心计算模块         │
│  ├── renderer.js (4502行) — 主交互+计算+展示       │
│  ├── tutorial.js (896行) — 教学系统                │
│  ├── extreme.js (197行) — 极值分析                  │
│  └── component_design.js (1207行) — 部件设计        │
│                                                    │
│  styles.css (2000+行)                               │
└──────────────────────────────────────────────────┘
```

### 1.2 进程模型

**主进程** (`main.js`):
- 创建 1 个 BrowserWindow，加载 `index.html`
- `nodeIntegration: true`, `contextIsolation: false`
- 注册 3 个 IPC handler：`save-excel-file`, `save-svg-file`, `save-pdf-file`
- 无 preload script，渲染器直接 `require('electron')`

**渲染进程**:
- 加载顺序：`calculations.js → tutorial.js → renderer.js → extreme.js → component_design.js`
- **无 ES modules**，所有函数以全局方式声明
- 依赖关系：后加载的模块依赖前加载模块定义的全局函数

---

## 2. 模块设计

### 2.1 calculations.js — 核心热力学计算

| 函数 | 行号 | 输入 | 输出 | 算法/公式 |
|------|:----:|------|------|-----------|
| `satPressure(T)` | 15 | T(℃) | P_sat(kPa) | Magnus(T≥0) / Sonntag(T<0)，GB/T 35226-2017 |
| `humidityRatio(P_sat,RH,P_atm)` | 29 | P_sat,RH,P_atm | W(kg/kg) | W=0.622·P_v/(P-P_v), P_v=RH/100·P_sat |
| `enthalpy(T,W)` | 43 | T,W | h(kJ/kg) | h=1.006·T+W·(2501+1.86·T) |
| `fmt(v,d)` | 48 | 数值,小数位 | 格式化字符串 | 负荷<1时自动多保留2位小数 |
| `virtualTemp(T_C,W)` | 62 | T,W | T_v(K) | T_v=T_K·(1+1.6078W)/(1+W) |
| `rhoMoistAir(P_kPa,T_C,W)` | 72 | P,T,W | ρ(kg/m³) | ρ=P/(R·T_v) |
| `calcHumidityRatio(T,RH,P_atm)` | 89 | T,RH,P_atm | W(kg/kg) | 合并 satPressure+humidityRatio |
| `calcWetBulb(P_v,t,P,A)` | 107 | P_v,t,P,A | {ts, P_sat_ts, ...} | Newton 迭代，手册 1.6-3 |
| `calcDewPoint(P_v_kPa)` | 149 | P_v | t_dp(℃) | Magnus 反推，GB/T 35226-2017 |
| `calcTemperatureFromW(W,RH,P_atm)` | 158 | W,RH,P_atm | T(℃) | Magnus 反推从含湿量求温度 |
| `calcCoilLoad(...)` | 179 | 质量流量,焓值,W_out,等 | {T_coil, h_coil, Q, Q_reheat, msgs} | 统一盘管负荷计算 |
| `getEngineeringParams()` | 202 | DOM | 工程参数对象 | 从 DOM 读取，三级 fallback |
| `calcCoilSectionLength(coilRows,rowSpacing)` | 233 | 排数,排距 | 长度(m) | 默认排距33mm+端板350mm |
| `calcXi(h1,h2,t1,t2,cp)` | 251 | h1,h2,t1,t2 | ξ | ξ=(h1-h2)/[cp·(t1-t2)]，手册湿工况 |
| `airProps(T)` | 268 | T(℃) | {λ, ν, Pr, cp} | 多项式拟合，手册空气物理性质表 |
| `rhoDryAir(T,P)` | 291 | T,P | ρ_dry(kg/m³) | P/(R·T_K) |
| `waterProps(T)` | 304 | T(℃) | {λ, ν, Pr} | 多项式拟合，IAPWS-IF97 |
| `calcAlphaAir(vy,de,xi,T)` | 328 | vy,de,ξ,T | αa(W/m²K) | Nu=0.27·Re^0.63·Pr^(1/3)·ξ^0.6 |
| `calcAlphaWater(w,di,T)` | 347 | w,di,T | αw(W/m²K) | Dittus-Boelter: Nu=0.023·Re^0.8·Pr^0.4 |
| `calcCoilK(vy,w,xi,rows,...)` | 376 | vy,w,ξ,rows | K(W/m²K) | K=a·v_y^n·ξ^p，手册经验公式表 |
| `getContactCoeff(vy,rows,tubeOD,finType)` | 401 | vy,rows,OD,finType | ε | ε=ε₀·vyCorr·odCorr·finCorr |
| `calcLogMeanEnthalpy(h1,h2,hb1,hb2)` | 445 | h1,h2,hb1,hb2 | Δh_m(kJ/kg) | 对数平均焓差 |

### 2.2 renderer.js — 主交互界面

| 功能 | 行号 | 描述 |
|------|:----:|------|
| `switchTab(tabName)` | 132 | 标签切换（6个标签页） |
| `buildPhysicsWarnings(data)` | 76 | 物理合理性校验 |
| `calculate(silent)` | 812 | **主计算入口** |
| `setPreset(season, event)` | 128 | 预设场景加载 |
| `onParamChange()` | 142 | 参数变化自动重算 |
| `buildAirStateSummary(data)` | 385 | 空气状态参数汇总 |
| `buildPhysicsExplanation(data)` | 228 | 物理意义解释 |
| `buildProcessSteps(data)` | 431 | 详细计算步骤 |
| `buildStructuralDesign(data)` | 681 | 结构设计与截面尺寸 |
| `generateSelection(data)` | 1130 | 设备选型建议 |
| `drawPsychroChart(data)` | 2827 | 绘制焓湿图 SVG |
| `exportReport()` | 2938 | Excel 报告导出 |
| `exportReportElectron()` | 3237 | Electron Excel 导出 |
| `exportSVG()` | 3407 | SVG 导出 |
| `exportPdf()` | 3434 | PDF 导出 |
| `toggleSymbolTable()` | 4504 | 参数符号表切换 |

### 2.3 component_design.js — 零部件详细设计

| 函数 | 行号 | 描述 |
|------|:----:|------|
| `syncFromDesign()` | 17 | 设计参数同步到主页 |
| `getTubeGeometry(tubeOD, variant)` | 40 | 管束规格查表 |
| `fillTubeGeometry()` | 81 | 管间距/排距自动填充 |
| `calcCoilByContactFactor(inp)` | 108 | **接触系数法盘管计算** |
| `importFromCalc()` | 264 | 从主计算导入参数 |
| `runCoilDesign()` | 332 | 表冷器详细设计 |
| `runHeaterDesign()` | 817 | 加热器设计 |
| `runHumidifierDesign()` | 883 | 加湿器设计 |
| `runFanDesign()` | 968 | 风机设计 |
| `buildDesignReport(title, sections)` | 575 | 设计报告 HTML |
| `showFanCircuit()` | 705 | 盘管回路示意图 |

### 2.4 extreme.js — 极值分析

| 函数 | 行号 | 描述 |
|------|:----:|------|
| `findWorstCorner(r, onEval)` | 13 | 边界扫描（2^5=32角点） |
| `analyzeCoil(r, ep)` | 35 | 盘管极值分析 |
| `analyzeHeater(r, ep)` | 117 | 加热器极值分析 |
| `analyzeHumidifier(r, ep)` | 162 | 加湿器极值分析 |
| `analyzeFan(r, ep)` | 198 | 风机极值分析 |
| `runExtremeAnalysis()` | 247 | 极值分析主流程 |

### 2.5 tutorial.js — 教学系统

| 功能 | 行号 | 描述 |
|------|:----:|------|
| `knowledgeData` | 80 | 知识点数据数组 |
| `beginnerSteps` | 283 | 初级教程步骤 |
| `advancedSteps` | 640 | 高级教程步骤 |
| `practiceSteps` | 771 | 练习步骤 |
| `switchGuideTab()` | 886 | 教程标签切换 |

---

## 3. 数据流设计

### 3.1 主计算数据流

```
用户输入 (DOM)
    │
    ▼
calculate(silent=false)
    │
    ├── 1. 读取输入: massFlow, tempIn, rhIn, tempOut, rhOut, P_atm=101.325
    │       + 高级参数: bypassFactor, coilRH, chwDeltaT, chwSupply
    │
    ├── 2. 输入验证: checkField() × 6 项
    │
    ├── 3. 计算入口状态: P_sat_in, P_v_in, W_in, h_in
    │
    ├── 4. 计算出口状态: P_sat_out, P_v_out, W_out, h_out
    │
    ├── 5. 负荷计算: Q_cooling, Q_heating, dW, dehumid
    │
    ├── 6. 工况判定: 降温除湿 / 升温加湿 / 等湿
    │
    ├── 7. 盘管负荷:
    │       ├── W_in > W_out → calcCoilLoad()
    │       ├── W_out > W_in && T_out ≥ T_in → 升温加湿(无制冷)
    │       ├── W_out ≥ W_in && T_out < T_in → 降温加湿(显热冷却)
    │       └── else → 等湿处理
    │
    ├── 8. 加湿量 + 冷冻水流量 + 电加热功率
    │
    ├── 9. 结构尺寸: 湿空气密度 → 体积流量 → 截面计算
    │
    ├── 10. 干湿球校核: calcWetBulb() × 2
    │
    ├── 11. 物理合理性校验: buildPhysicsWarnings()
    │
    └── 12. 显示: results, airStateSummary, physicsExplanation, structuralDesign, calcProcess
```

### 3.2 表冷器设计数据流

```
主计算参数
    │
    ▼
importFromCalc()
    │
    ├── 复制基础参数到设计页
    ├── 计算 W_in, W_out, h_in, h_out
    └── 按工况分支计算 T_coil, Q_coil, Q_reheat
    │
    ▼
runCoilDesign()
    │
    ├── 读取输入 + 迎面风速
    ├── 迎面尺寸: A_face = Q_v/v, W=√(A·λ), H=A/W
    │
    ├── LMTD法:
    │       ├── ΔT₁ = T_in - T_chw_out
    │       ├── ΔT₂ = T_out - T_chw_in
    │       ├── LMTD = (ΔT₁-ΔT₂)/ln(ΔT₁/ΔT₂)
    │       ├── ξ = calcXi(h1,h2,T1,T2)
    │       ├── K = calcCoilK(vy, w, ξ, rows) — 迭代
    │       └── A_req = Q×1000/(K×LMTD)×F_foul
    │
    ├── ε法:
    │       ├── ts1, ts2 = calcWetBulb() × 2
    │       ├── ε = 1 - (tg₂-ts₂)/(tg₁-ts₁)
    │       ├── ξ = calcXi(i1, i_coil, T1, T_coil_out)
    │       └── rows = f(ε, vy, tubeOD, finType)
    │
    ├── 结构参数: 管数, 回路, 流速, 外形尺寸
    └── 显示设计报告
```

---

## 4. 界面设计

### 4.1 标签页

| 标签 | 功能 | 布局 |
|:----|:-----|:----:|
| 参数计算 | 主计算：输入+结果+焓湿图 | 上下排列 |
| 设计指南 | 从零到精通的分步教程 | 左右+侧边栏 |
| 知识库 | 核心知识点汇总 | 左右（目录+内容） |
| 设备选型建议 | 基于结果推荐设备 | 单列卡片 |
| 在线问答 | AI 助手 / FAQ 搜索 | 单列聊天 |
| 零部件设计 | 表冷器/加热器/加湿器/风机详细设计 | 上下排列 |

### 4.2 参数符号表

浮动面板，点击标签栏右侧"📖 参数符号"按钮弹出。覆盖底层遮罩，点击外部或 ✕ 关闭。
分 5 类：空气状态参数、负荷参数、表冷器/盘管参数、结构尺寸参数、物理常数。

---

## 5. 外部依赖

| 依赖 | 用途 |
|------|------|
| Electron 33.4.0 | 桌面应用框架 |
| electron-builder 24.13.3 | 打包工具 |
| xlsx | Excel 导出 |

---

## 6. 参考标准

- GB/T 35226-2017《湿空气性质计算公式》— 饱和水汽压、含湿量、焓值
- GB/T 14294-2026《组合式空调机组》— 迎面风速、结构设计
- GB 50736-2012《民用建筑供暖通风与空气调节设计规范》— 管内流速
- 《实用供热空调设计手册》第2版 — 湿球温度(1.6-3)、空气物性表、表冷器传热系数
- 《空气调节设计手册》第三版 — 接触系数法、表冷器设计
- ASHRAE Handbook Fundamentals — 空气/水物性校验
- GB/T 14689-2008《技术制图 图纸幅面和格式》— SVG 导出标准

---

## 修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|----------|--------|
| V1.0 | 2026-06-27 | 初始版本 | AI |
